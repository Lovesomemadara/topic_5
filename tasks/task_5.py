# Ввод строки
nums: str = input("Введите строку: ")

# Объявление переменных
num_min: str = min(nums)
num_max: str = max(nums)

print(
    "Минимальный символ:", num_min, "\n"
    "Максимальный символ:", num_max
)
